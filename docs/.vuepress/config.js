module.exports = {
    // 标题
    title: 'walkjs的文档',
    // 描述
    description: 'walkjs 文档描述',
    // 输出目录
    dest: './dist',
    // 设置站点根路径
    base: '/docs-generator/',
    // 需要插入到 head 标签的内容
    head: [
        ['link', { rel: 'icon', href: '/logo.png' }]
    ]
}
